{
  "name": "all-origins-proxy",
  "version": "1.0.0",
  "description": "Simple all-origins CORS proxy with monitoring frontend (in-memory stats). Use with caution.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.18.2",
    "express-rate-limit": "^6.7.0"
  },
  "devDependencies": {
    "jest": "^30.1.1",
    "supertest": "^7.1.4"
  }
}
